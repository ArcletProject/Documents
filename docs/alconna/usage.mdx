---
id: usage
title: 应用
---

## 奇奇怪怪的命令

`Alconna` 的 `command`支持写入正则表达式, 所以以下命令是可以的

import { TerminalApp } from "@site/src/components/Teminal.js";

```python
w = Alconna(command=f"查询{AnyStr}天气")
d = Alconna(headers=["."], command=f"d{AnyDigit}")
```

import {usage1, usage2, usage3, usage4} from "@site/src/components/commandlines.js";

<TerminalApp lines={usage1} height={200}/>

## 选项的多参数

`Option` 与 `Subcommand` 的 `args` 可以填入不止一个参数，所以以下命令是可以的

```python
cal = Alconna(
    command="Cal",
    options=[
        Option("-sum", Args["num_a":int, "num_b":int]),
        Subcommand(
            "-div",
            Option("--round", Args["decimal":int]),
            args=Args["num_a":int, "num_b":int]
        )
    ]
)
```

<TerminalApp lines={usage2} height={240}/>

> PS. Alconna在解析完成后会把破折线给过滤掉

## 自定义分隔符

`Alconna` 不强制 shell-like 的指令，所以以下命令是可以的

```python
alc = Alconna(
    command=f"{AnyStr}今天",
    main_args=Args["item":AnyParam],
    actions=lambda x: x.split("和")
).separate("吃")
```

<TerminalApp lines={usage3} height={200}/>

> PS. 如果你想用空字符串作为分隔符的话,为什么不试试第一种写法呢？

## 非文本元素作为命令头

注: 该特性为`0.5.3`以上

`headers`可以传入消息元素, 所以以下是可以的:
```python
test = Alconna(
    headers=[At(12345)],
    command="丢漂流瓶",
    main_args=Args["content":AnyParam]
)
```

<TerminalApp lines={usage4} height={200}/>